

clc
clear

eq = load('EQ-4.csv');

t = eq(:,1);

u1 = eq(:,2);
alpha1 = 140;

u2 = eq(:,3);
alpha2 = 230;

strike = 10;

thetax = 0;
thetay = 90;

idx = 1;
for i = 0:1:360
    
    beta1 = (i - alpha1 - thetax)*pi/180;
    beta2 = (i - alpha2 - thetay)*pi/180;
    
    
    u_fp = u1*cos(beta1) + u2*cos(beta2);
    u_fn = u1*sin(beta1) + u2*sin(beta2);
    
    % figure(1)
    % plot(t, u_fp);
    % xlabel('Time (sec)')
    % ylabel('Acceleration FP')
    % grid on
    %
    % figure(2)
    % plot(t, u_fn);
    % xlabel('Time (sec)')
    % ylabel('Acceleration FN')
    % grid on
    maxufp(idx) = max(abs(u_fp));
    maxufn(idx) = max(abs(u_fn));
    idx = idx + 1;
    
    sprintf('FP: %8.4f g | Paper = %8.4f g || FN: %8.4f g | Paper = %8.4f g', [max(abs(u_fp)) 0.3 max(abs(u_fn)) 0.5])
    
end

i = 0:1:360;
figure(3)
subplot(2,1,1)
plot(i, )
